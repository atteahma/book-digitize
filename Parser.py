class Parser:
    """
    in: raw text and metadata from tesseract    \n
    out: file with text (pdf,txt,etc)
    """
    
    def __init__(self):
        pass

    def createFileFromText(self, text, filetype):
        if filetype == 'txt':
            # do something
            return

        if filetype == 'pdf':
            # do something
            return

        return